# ============================================================
# 核心依赖 · 跑回测 + 看板 + 全套测试 都需要这些
# ============================================================

# ---- 数值与数据 ----
numpy==2.2.2
pandas==2.2.3
pyarrow>=18.0.0        # Parquet 列式存储
pyyaml>=6.0.2          # config.yaml 配置

# ---- 前端可视化 ----
streamlit>=1.39.0
plotly>=5.24.0

# ---- 测试 ----
pytest>=8.3.0
hypothesis>=6.0.0      # 属性测试（tests/property/）
pytest-cov>=4.1.0      # 覆盖率（pytest --cov）
pytest-benchmark>=4.0.0   # 性能基准（tests/perf/）
